<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.dromel.proyectfinaldes6">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".TareaNuevaActivity"></activity>
        <activity android:name=".TareaActualizarActivity" />
        <activity android:name=".ComentsActivity" />
        <activity android:name=".RegistrarLogin" />
        <activity android:name=".actualizarTareaActivity" />
        <activity android:name=".TareasActivity" />
        <activity android:name=".IngresarLogin">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>